apiVersion: v1
kind: Secret
metadata:
  name: reservations-secret
  namespace: reservations-prod
type: Opaque
# Values are base64-encoded. Generate with: echo -n 'value' | base64
# In real production: use Sealed Secrets, Vault, or inject via CI pipeline — never commit real credentials.
data:
  SPRING_DATASOURCE_USERNAME: cmVzZXJ2YXRpb25z   # reservations
  SPRING_DATASOURCE_PASSWORD: CHANGE_ME_BASE64     # replace before deploying
  POSTGRES_PASSWORD: CHANGE_ME_BASE64              # replace before deploying
